import React from "react";
import { render, screen } from "@testing-library/react";
import SearchItem from "./SearchItem";

test("renders Home", () => {
  // render(<SearchItem searchResult={undefined} />);
  //
});
